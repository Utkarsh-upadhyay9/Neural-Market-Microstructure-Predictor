# Docker Deployment

## Quick Start
```bash

# Build and run
docker-compose up --build

#Or:
# Build image
docker build -t neural-market-predictor .

# Run container
docker run -p 8000:8000 neural-market-predictor

# Access at http://localhost:800
